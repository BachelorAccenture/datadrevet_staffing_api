spring:
  config:
    activate:
      on-profile: test

  neo4j:
    uri: bolt://will-be-overridden
    authentication:
      username: neo4j
      password: password

  main:
    allow-bean-definition-overriding: true

logging:
  level:
    org.springframework.security: DEBUG